// GIT COMANDOS BASICOS //

/*  git init
    git add <file>
    git status
    git commit
    git push
    git pull
    git clone  */

    /* iniciando git en una carpeta del escritorio (ya con index.html y js)

    abrimos git.bash y ponemos:

    - pwd (para que nos muestre las opciones de escritorio)
    - cd 'nombre de la carpeta a la cual queremos entrar'
    - ls (para que nos muestre el contenido adentro de la carpeta)
    - git init (para avisarle que vamos a iniciar el sistema de control de versiones)

    // a continuación en la carpeta que iniciamos git nos va a aparecer una carpeta oculta llamada .git (donde no tenemos que tocar nada) //

    - git status (para ver que archivos tenemos o cuales estamos trabajando y tambien ver que archivos estan en el aire, que todavia no los agregamos al area de trabajo)
    - git add (index.html o codigo.js) (para agregar al area de trabajo y decirle que esos archivos o un archivo conforma la primer versión de mi programa)
    
    // si ponemos git status devuelta, vamos a ver que ya estamos trabajando con los archivos por ende nos aparecerian en verde y no en rojo //

//---------------------------------------------------------------------------------------------------------//

//Mi primer reposito//

//para limpiar la terminal usaremos 'cls'

- abrimos terminal en VSC (ya teniendo index.html etc)
- git config --global user.name "lucas" (para registrarnos - entre comillas iria nuestro nombre)

// si ponemos git config --global user.name, sin nada mas nos aparecia nuestro nombre registrado //

- git config --global user.email "luquiroga420@gmail.com" (email de github)

// si ponemos git config --global user.email, sin nada mas nos aparecia nuestro email registrado //

- git init (para empezara  trabajar con este proyecto)
- git add . (para agregar cargar/registrar todo los archivos al repositorio, html,js,css)
- git commit -m "mi primer commit" (forma para sacarle una foto a nuestros cambios y los registramos con un nombre para a futuro indentificar lo que nosotros hicimos en nuestro proyecto - entre comillas iría el nombre)
- git log --oneline (para visualizar todos los commit que trabajamos en nuestra computadora)




















 